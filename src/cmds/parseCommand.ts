// import commands from './commands';

// function isOk(command: string): boolean {
//   return command.toLowerCase().trim().startsWith('/');
// }
//
// export default function parseCommand(command: string): string {
//   if (!isOk(command)) {
//     return 'Command not correct';
//   } else {
//     return command.toLowerCase().trim().slice(1);
//   }
// }
